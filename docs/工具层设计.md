# 工具层设计（绘图部分 · 已收敛）

> **现行真源**：[`定稿-绘图子系统.md`](./定稿-绘图子系统.md)  
> 下文旧 Tier-A/B/C 表已作废。2026-09-11 起：

- **唯一 Agent 出图工具**：`draw_with_workflow`
- **仅 8 种 kind**：`logic_dag` / `timing_wave` / `state_machine` / `truth_table` / `kmap` / `seven_seg` / `char_curve` / `msi_design`
- **已删除**：`design_circuit`、`msi_counter`、直调 `draw_kmap` 等、xor/门符号工具、`draw_diagram`、`draw_schematex`、Schematex DSL 沙箱（`tools_runtime/schematex`）

内部仍由工作流 runners 调用 Python 引擎（kmap/truth/seven/curve）与 Node/Digital，**不对 Agent 再暴露第二套工具名**。
