# 提示词：意图识别与智能路由

> 权威：`backend/app/agents/intent_router.py`  
> 路由节点先做**意图判定**（短/长、拦截）；**仅长任务**才拆原子题清单。  
> 短任务（`react_short`）→ 直达 **ReAct**（有工具链）。  
> 长任务（`plan_execute`）→ Plan HITL → 子任务逐个 **ReAct**。  
> **不做**「默认无工具直出」。`semantic_router.route_task` 降级时整段一条，无规则拆题。服务端仅截断本回合执行上限 5。
